spring:
  application:
    name: grpc-enrichment-client

  # RabbitMQ
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        retry:
          enabled: true
          initial-interval: 2000    # первая повторная попытка через 2 сек
          max-attempts: 3           # максимум 3 попытки
          multiplier: 2.0           # каждая следующая x2 (2с → 4с → 8с)

# HTTP-порт для Spring Boot (actuator, health)
# Не конфликтует с demo-rest (8080), audit-service (8081), metadata-server (8083)
server:
  port: 8082

# gRPC-клиент
# Адрес gRPC-сервера (grpc-metadata-server)
grpc:
  client:
    metadata-server:
      host: localhost
      port: 9090

# Actuator — проверка здоровья сервиса
management:
  endpoints:
    web:
      exposure:
        include: health,info
